e<template>
  <div
    class="skill-card animate-on-scroll opacity-0 transform translate-y-8 transition-all duration-700"
  >
    <div
      class="group relative bg-white dark:bg-slate-800 rounded-2xl p-8 shadow-lg hover:shadow-2xl border border-slate-200 dark:border-slate-700 hover:border-transparent transition-all duration-300 overflow-hidden"
    >
      <!-- Gradiente de fondo animado -->
      <div
        class="absolute inset-0 opacity-0 group-hover:opacity-10 transition-opacity duration-300 rounded-2xl"
        :class="`bg-gradient-to-br ${skill.color}`"
      ></div>

      <div class="relative z-10">
        <!-- Icono y título -->
        <div class="flex items-center mb-6">
          <div
            class="text-3xl mr-4 group-hover:scale-110 transition-transform duration-300"
          >
            {{ skill.icon }}
          </div>
          <h3
            class="text-xl font-bold text-slate-900 dark:text-white mb-3 group-hover:text-blue-600 dark:group-hover:text-blue-400 transition-colors duration-300"
          >
            {{ skill.category }}
          </h3>

          <p class="text-slate-600 dark:text-slate-400 mb-4 leading-relaxed">
            
          </p>

          <!-- Tecnologías -->
          <div class="flex flex-wrap gap-2 mb-4">
            <span
              v-for="tech in skill.technologies"
              :key="tech"
              class="px-3 py-1 text-sm font-medium bg-blue-100 dark:bg-blue-900/30 text-blue-800 dark:text-blue-300 rounded-full hover:bg-blue-200 dark:hover:bg-blue-900/50 transition-colors duration-200"
            >
              {{ tech }}
            </span>
          </div>

          <!-- Enlaces en el contenido para móvil -->
          <div class="flex gap-3 sm:hidden">
            <a
              
              
              target="_blank"
              class="flex items-center gap-2 text-slate-600 dark:text-slate-400 hover:text-slate-900 dark:hover:text-slate-200 transition-colors duration-200"
            >
              <GithubIcon class="w-4 h-4" />
              <span class="text-sm font-medium">Código</span>
            </a>
            <a
              
              
              target="_blank"
              class="flex items-center gap-2 text-slate-600 dark:text-slate-400 hover:text-slate-900 dark:hover:text-slate-200 transition-colors duration-200"
            >
              <ExternalLinkIcon class="w-4 h-4" />
              <span class="text-sm font-medium">Demo</span>
            </a>
          </div>
        </div>
      </div>
    </div>
  </div>
</template>

<script setup lang="ts">
interface SkillType {
  category: string;
  icon: string;
  technologies: string[];
  color: string;
}

interface Props {
  skill: SkillType;
  delay?: number;
}

defineProps<Props>();
</script>
